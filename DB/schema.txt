Account (USERNAME, PASSWORD, PROFILE_NAME, EMAIL, IMAGE_URL);
Category (TYPE,POINTS);
Dish (ID, NAME, DESCRIPTION,TOTAL_RATING, IMAGE_URL, CATEGORY*);
RATING ((USERNAME*, DISH*), POINTS);
Comment ((USERNAME*, DISH*, DATA), TEXT);


